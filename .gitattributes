# Vendored third-party code is pinned by the sha256 recorded next to loadPeerJS()
# in index.html. Line-ending normalisation would rewrite those bytes on checkout
# and the hash would stop matching, so leave this directory exactly as fetched.
vendor/** -text
